# Kernel source (large, has its own repo)
kernel/

# Binary images (too large for git, not source)
*.img
*.bin
*.zip
*.cpio.gz

# Build artifacts
kernel_build.log
out/

# Ramdisk / initramfs (extracted and patched by build-bootimg.sh)
halium-initramfs/
halium_arm64.tar.xz

# Claude Code
.claude/

# macOS
.DS_Store
