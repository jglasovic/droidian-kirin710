[Service]
# Replace one-shot activate with a polling script that waits for UDC
# (handles cable connected after boot — adbd-usb-gadget activate doesn't retry)
ExecStartPost=
ExecStartPost=/usr/local/sbin/adbd-udc-wait.sh
